import styled from 'styled-components';

export const ParanjaWrapper = styled.section`
    width: 100%;
    height: 100%;
    z-index: 1500;
    position: absolute;
    background: rgba(0, 0, 0, .5);
    max-width: 1260px;
`;
